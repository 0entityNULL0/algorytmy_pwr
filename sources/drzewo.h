template <typename T> drzewo_zbalansowane
{
private:
	struct wezel
	{
		T *dane;
		wezel *w1;
		wezel *w2;
	}
	*(int porownanie(T,T);)
	wezel *korzen;
public:
	int dodaj(T& element)
	int szykaj_elementu(T element);
	int szykaj_elementu(int czy_rowne(T,T));
	T* usun element();
};
